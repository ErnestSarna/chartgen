# Inference + chart generation. Verified on Python 3.14 (Windows).
#
# torch/torchaudio are deliberately NOT pinned here — install them first from the
# right index for the machine, because plain `pip install torch` on Windows gives
# a CPU-only build and would silently waste a GPU:
#
#   CPU:   pip install torch==2.9.1 torchaudio==2.9.1 --index-url https://download.pytorch.org/whl/cpu
#   CUDA:  pip install torch==2.9.1 torchaudio==2.9.1 --index-url https://download.pytorch.org/whl/cu124
#
# setup.ps1 does this for you.

transformers==4.57.1
huggingface-hub==0.36.0
tokenizers==0.22.2
safetensors==0.8.0

librosa==0.11.0
soundfile==0.14.0
soxr==1.1.0
audioread==3.1.0
numba==0.66.0
llvmlite==0.48.0
scipy==1.18.0
scikit-learn==1.9.0
numpy==2.4.6
tqdm==4.70.0

# YouTube link -> chart. imageio-ffmpeg carries a static ffmpeg binary, so no
# system ffmpeg install is needed for the download/convert path.
yt-dlp==2026.7.4
imageio-ffmpeg==0.6.0

# .mid chart conversion for the training corpus (tools/mid2chart.py).
mido==1.3.3

# Artist/title autofill from audio file tags (ID3/Vorbis/MP4).
mutagen==1.48.1

# Lyric transcription -> CH lyric events (small model on GPU, base on CPU;
# models download from HF on first use and cache).
faster-whisper==1.2.1
