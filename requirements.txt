# Inference + chart generation. Verified on Python 3.14 (Windows).
#
# torch/torchaudio are deliberately NOT pinned here — install them first from the
# right index for the machine, because plain `pip install torch` on Windows gives
# a CPU-only build and would silently waste a GPU:
#
#   CPU:   pip install torch==2.9.1 torchaudio==2.9.1 --index-url https://download.pytorch.org/whl/cpu
#   CUDA:  pip install torch==2.9.1 torchaudio==2.9.1 --index-url https://download.pytorch.org/whl/cu126
#
# basic-pitch is also NOT here: its wheel declares `tensorflow<2.15.1` on Windows
# for Python >= 3.11, which has no build and makes pip refuse to install it. It
# is installed with --no-deps (the ONNX backend needs only onnxruntime, listed
# below). setup.ps1 does all of this for you.

# --- note transcription (Basic Pitch, Apache-2.0; runs on the bundled ONNX model)
onnxruntime==1.28.0
mir_eval==0.8.2
pretty_midi==0.2.11.post0
resampy==0.4.3
typing_extensions==4.15.0
mido==1.3.3

# --- stem separation: BS-RoFormer (MIT code, default) with Demucs (MIT) as fallback
bs-roformer-infer==0.1.5
ml_collections==1.1.0
PyYAML==6.0.3
einops==0.8.2
rotary-embedding-torch==0.9.1
beartype==0.22.9
demucs==4.1.0
julius==0.2.8
lameenc==1.8.4
pyloudnorm==0.2.0

# --- lyrics: LRCLIB lookup, faster-whisper (MIT) fallback, torchaudio forced alignment
faster-whisper==1.2.1
ctranslate2==4.8.1
transformers==4.57.1
huggingface-hub==0.36.0
tokenizers==0.22.2
safetensors==0.8.0
requests==2.34.2

# --- audio analysis
librosa==0.11.0
soundfile==0.14.0
soxr==1.1.0
audioread==3.1.0
numba==0.66.0
llvmlite==0.48.0
scipy==1.18.0
scikit-learn==1.9.0
joblib==1.5.3
numpy==2.4.6
tqdm==4.70.0

# --- YouTube link -> chart. imageio-ffmpeg carries a static ffmpeg binary, so no
# system ffmpeg install is needed for the download/convert path.
yt-dlp==2026.7.4
imageio-ffmpeg==0.6.0

# --- artist/title autofill from audio file tags, and embedded album art
mutagen==1.48.1
pillow==12.3.0

# --- UIs: Fluent/Win11 look for the Tkinter desktop app; Flask for the remote web UI
sv-ttk==2.6.1
Flask==3.1.3
